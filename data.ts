import { Category, Group, Entry } from './types';

// This file defines the initial state of the application.
// When the application starts for the first time, it will be empty.
// User data is persisted in the browser's LocalStorage, separate from the application code.

export const initialCategories: Category[] = [];

export const initialGroups: Group[] = [];

export const initialEntries: Entry[] = [];
